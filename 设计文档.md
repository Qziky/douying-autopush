# 抖音自动化内容生产系统（个人版）全套文档

------

# 一、产品需求文档（PRD）

## 1. 项目概述

### 1.1 项目背景

个人抖音账号运营中，「追热点-想文案-做视频-发作品」是高频且重复的劳动流程，存在以下痛点：

- 热点时效性强，人工跟进耗时费力，容易错过最佳发布窗口
- 视频制作流程标准化程度高，口播+字幕+配图的形式可被自动化替代
- 发布操作机械重复，占用大量创作时间
- 个人运营精力有限，无法高频次、多账号持续产出内容

### 1.2 项目目标

打造一套面向个人运营的全自动化内容生产流水线，实现「热点自动发现→文案自动生成→视频自动渲染→作品自动发布」的闭环，大幅降低内容生产的人力成本，提升热点响应速度与发布效率。

### 1.3 产品定位

- **使用对象**：个人抖音创作者、自媒体运营者
- **使用场景**：资讯类、科普类、盘点类等标准化口播视频的批量生产
- **核心价值**：效率提升、热点即时响应、释放创作精力
- **合规边界**：仅用于个人学习与非盈利运营，严格遵守抖音社区规范与第三方服务使用条款

## 2. 核心功能架构

产品采用「流水线单向流转+横切通用支撑」的架构，共6大功能模块：

1. 热点分析模块：热点采集 + 智能选题
2. 文案生成模块：脚本创作 + 分镜设计
3. 视频渲染模块：素材匹配 + 视频合成
4. 抖音适配器模块：账号管理 + 风控 + 自动发布
5. 调度触发模块：定时/手动/实时触发
6. 通用支撑模块：配置、日志、合规校验、异常重试

## 3. 功能模块详细说明

### 3.1 热点分析模块

#### 3.1.1 热点采集子模块

- 支持抓取抖音网页版热点榜实时数据
- 提取字段：排名、热点标题、热度值、话题标签、分类
- 支持配置刷新频率，自动更新热点数据
- 输出标准化的原始热点列表

#### 3.1.2 热点选题子模块

- 支持按账号定位配置选题规则（领域分类、热度阈值、排除关键词）
- 自动过滤敏感、违规、低价值热点
- 对候选热点按热度、匹配度、上升潜力综合排序
- 输出TOP N个最终创作选题

### 3.2 文案生成模块

- 接收热点选题，调用大模型生成口播文案
- 支持配置文案风格、时长、人设
- 自动拆分分镜，为每句台词匹配画面关键词
- 同步生成视频标题、话题标签、封面文案
- 输出结构化的标准视频脚本
- 内置文案字数校验，确保与视频时长匹配

### 3.3 视频渲染模块

#### 3.3.1 素材服务子模块

- 支持根据画面关键词自动搜索免费可商用素材库
- 本地素材缓存机制，重复素材无需重复下载
- 支持素材格式、分辨率统一预处理
- 支持本地素材库优先级配置

#### 3.3.2 视频合成子模块

- 自动生成自然语音口播（支持多音色选择）
- 自动生成精准对齐的动态字幕
- 自动匹配画面素材，添加转场特效
- 统一输出指定分辨率、帧率的MP4文件
- 自动截取推荐封面图

### 3.4 抖音适配器模块

#### 3.4.1 账号管理子模块

- 支持扫码登录，自动持久化Cookie
- 登录态自动检测与刷新
- 支持多账号配置与切换

#### 3.4.2 风控子模块

- 操作随机延迟，模拟人类行为
- 鼠标轨迹模拟，避免机械操作
- 发布频率限流，单日发布量可控
- 异常操作拦截与告警

#### 3.4.3 发布执行子模块

- 自动上传视频文件
- 自动填写标题、话题标签、封面
- 支持配置发布时间（立即/定时）
- 返回发布结果与作品链接

### 3.5 调度触发模块

- 定时触发：支持Cron表达式配置每日执行时间
- 手动触发：支持命令行单条执行全流程
- 热点监控触发：热点榜Top3变化时自动触发创作
- 任务状态跟踪与失败重试

### 3.6 通用支撑能力

- 配置管理：统一配置文件，所有参数集中管理
- 日志系统：全链路运行日志，支持分级输出
- 内容合规校验：文案、标题、字幕敏感词检测
- 异常重试：网络请求、API调用自动重试

## 4. 核心业务流程

```
触发执行 → 抓取热点榜 → 筛选最终选题 → 生成结构化脚本 → 内容合规校验
    → 匹配素材+生成语音+合成视频 → 账号登录态检测 → 风控校验
    → 上传发布 → 记录结果 → 流程结束
```

## 5. 非功能需求

1. **稳定性**：单条流程成功率≥90%，模块间解耦，单点失败不影响全局
2. **可维护性**：模块职责单一，配置与代码分离，页面元素选择器集中管理
3. **合规性**：所有素材使用CC0协议资源，内容自动合规校验，操作模拟人类行为
4. **性能**：单条60秒视频渲染耗时≤2分钟，全流程耗时≤5分钟
5. **轻量性**：个人本地运行，无需服务器部署，资源占用低

## 6. 产品边界与风险说明

- **不保证内容效果**：工具仅负责自动化生产，不保证播放量、涨粉等运营效果
- **账号风险**：自动化操作存在账号限流、封禁风险，需谨慎使用小号测试
- **内容质量**：自动生成内容需人工审核，避免低质、违规内容发布
- **版本兼容**：抖音前端改版、API变更可能导致功能失效，需定期维护

------

# 二、技术设计文档

## 1. 总体技术架构

采用四层分层架构+横切通用支撑组件，模块间单向数据流转，低耦合高内聚。

```
flowchart TD
    %% 调度触发层
    subgraph 调度触发层
        direction LR
        S1[定时调度<br/>APScheduler]
        S2[手动触发入口]
        S3[热点实时监控]
    end

    %% 核心业务层 - 主流水线
    subgraph 核心业务层
        direction LR
        %% 热点分析模块
        subgraph 热点分析模块
            H1[热点采集子模块<br/>HotCrawler]
            H2[热点选题子模块<br/>HotSelector]
            H1 --> H2
        end

        %% 文案生成
        C[文案生成模块<br/>ContentGenerator]
        
        %% 合规校验节点
        V[内容合规校验<br/>ComplianceCheck]

        %% 视频渲染模块
        subgraph 视频渲染模块
            R1[素材服务<br/>MaterialService]
            R2[视频合成器<br/>VideoComposer]
            R1 --> R2
        end

        %% 抖音适配器模块
        subgraph 抖音适配器模块
            A1[账号管理子模块<br/>AccountManager]
            A2[风控子模块<br/>RiskController]
            A3[发布执行子模块<br/>VideoPublisher]
            A1 --> A3
            A2 -.行为控制.-> A3
        end

        %% 主流水线流向
        H2 --> C --> V --> R2 --> A3
    end

    %% 基础能力层
    subgraph 基础能力层
        direction LR
        B1[大模型API<br/>LLM]
        B2[语音合成引擎<br/>TTS]
        B3[素材库API<br/>Stock Media]
        B4[浏览器自动化<br/>Playwright]
    end

    %% 数据存储层
    subgraph 数据存储层
        direction LR
        D1[配置文件<br/>JSON/YAML]
        D2[运行日志<br/>Logging]
        D3[素材缓存<br/>本地文件]
        D4[作品数据记录<br/>SQLite]
    end

    %% 通用支撑组件（横切所有层）
    subgraph 通用支撑组件
        U1[配置管理]
        U2[日志系统]
        U3[异常重试]
        U4[工具函数集]
    end

    %% 层级触发关系
    调度触发层 --> 热点分析模块
    
    %% 基础能力向上支撑
    B1 -.能力支撑.-> C
    B2 -.能力支撑.-> R2
    B3 -.能力支撑.-> R1
    B4 -.能力支撑.-> 抖音适配器模块

    %% 数据存储读写关系
    D1 -.读取配置.-> 通用支撑组件
    D2 -.写入日志.-> 通用支撑组件
    D3 -.素材读写.-> R1
    D4 -.结果落库.-> A3

    %% 通用支撑横切所有业务层
    通用支撑组件 -.横切赋能.-> 调度触发层
    通用支撑组件 -.横切赋能.-> 核心业务层
    通用支撑组件 -.横切赋能.-> 基础能力层
```

## 2. 分层技术设计

### 2.1 调度触发层

- **核心职责**：任务编排与触发，不处理具体业务逻辑
- **技术实现**：APScheduler 作为调度内核，支持多触发器共存
- **任务粒度**：全流程任务、单模块任务可独立触发
- **状态管理**：任务执行状态、重试次数本地记录

### 2.2 核心业务层

#### 热点分析模块

- 技术栈：Requests + BeautifulSoup4 + Playwright（备选）
- 热点采集：HTTP请求抓取热榜页面，DOM解析提取结构化数据
- 热点选题：规则引擎 + 简单评分算法，可配置权重
- 设计原则：采集逻辑与决策逻辑严格分离

#### 文案生成模块

- 技术栈：官方SDK + Prompt模板引擎
- 核心逻辑：模板化Prompt + 结构化输出约束
- 输出格式：强制JSON格式，便于下游解析
- 设计原则：纯内容创作，不涉及任何音视频处理

#### 视频渲染模块

- 技术栈：edge-tts + MoviePy / FFmpeg + Requests
- 语音合成：离线/在线TTS，生成带时间戳的字幕
- 素材匹配：关键词搜索素材库API，自动下载缓存
- 视频合成：按分镜拼接素材，叠加字幕与音频
- 设计原则：素材管理与渲染逻辑分离，只执行不创作

#### 抖音适配器模块

- 技术栈：Playwright 浏览器自动化
- 账号管理：Cookie持久化到本地文件，启动时自动加载
- 风控引擎：操作前注入随机延迟、鼠标移动轨迹
- 发布执行：页面元素操作封装，模拟人工上传流程
- 设计原则：账号管理、风控、发布三者解耦

### 2.3 基础能力层

以接口形式封装底层能力，向上层提供统一调用入口：

- 大模型接口：统一封装多家LLM调用，支持切换
- TTS接口：统一语音合成接口，支持多引擎切换
- 素材接口：统一素材搜索下载接口，支持多素材源
- 浏览器接口：统一浏览器实例管理，复用登录态

### 2.4 数据存储层

- 配置文件：YAML格式，分层配置（默认+用户自定义）
- 日志文件：按天切割，分级输出（INFO/ERROR/DEBUG）
- 素材缓存：按关键词哈希存储，自动过期清理
- 作品数据：SQLite轻量数据库，存储发布记录与基础数据

### 2.5 通用支撑组件

以工具类/装饰器形式提供，横切所有业务模块：

- 配置管理器：单例模式，统一读取配置
- 日志工厂：统一日志格式与输出目标
- 重试装饰器：支持自定义重试次数、间隔、异常类型
- 合规校验器：敏感词匹配 + LLM二次校验

## 3. 核心数据契约

使用 Python `dataclasses` 定义标准化数据结构，模块间仅通过标准对象交互。

```
from dataclasses import dataclass
from typing import List, Optional

@dataclass
class HotItem:
    """原始热点项"""
    rank: int
    title: str
    hot_value: int
    category: str
    url: str
    tags: List[str]

@dataclass
class SelectedTopic:
    """最终选题"""
    hot_item: HotItem
    score: float
    create_angle: str
    risk_level: int  # 0低危 1中危 2高危

@dataclass
class Scene:
    """单条分镜"""
    index: int
    text: str          # 口播台词
    image_keyword: str # 画面关键词
    duration: float    # 时长(秒)

@dataclass
class VideoScript:
    """标准化视频脚本"""
    topic: SelectedTopic
    title: str
    full_script: str
    tags: List[str]
    scenes: List[Scene]
    total_duration: int
    cover_text: str

@dataclass
class RenderResult:
    """渲染结果"""
    video_path: str
    cover_path: str
    duration: float
    resolution: tuple

@dataclass
class PublishResult:
    """发布结果"""
    success: bool
    video_url: Optional[str]
    publish_time: Optional[str]
    error_msg: Optional[str]
```

## 4. 技术选型与说明

| 技术领域     | 选型                           | 版本要求                   | 选型理由                                   |
| ------------ | ------------------------------ | -------------------------- | ------------------------------------------ |
| 开发语言     | Python                         | 3.10+                      | 自动化、AI生态最完善，语法简洁适合个人项目 |
| 调度框架     | APScheduler                    | 3.10+                      | 轻量无侵入，支持Cron、日期、间隔多种触发器 |
| 网页爬虫     | Requests + BeautifulSoup4      | 最新稳定版                 | 轻量高效，满足热榜抓取需求                 |
| 浏览器自动化 | Playwright                     | 最新稳定版                 | 自动管理驱动，元素定位稳定，反检测能力强   |
| 大模型       | OpenAI格式，自行接入           | -                          | 接口规范                                   |
| 语音合成     | edge-tts                       | 最新稳定版                 | 微软免费语音，效果自然，无需API密钥        |
| 视频处理     | MoviePy（入门） FFmpeg（进阶） | MoviePy 1.0.3+ FFmpeg 4.4+ | MoviePy入门简单；FFmpeg性能高、特效丰富    |
| 素材库       | Pexels API                     | -                          | 免费可商用，图片视频资源丰富，API友好      |
| 数据存储     | SQLite + JSON/YAML             | -                          | 轻量无需部署，满足个人项目数据存储需求     |
| 日志系统     | logging（标准库）              | -                          | Python内置，无需额外依赖，功能完备         |

## 5. 关键技术方案

### 5.1 账号登录态维护方案

1. 首次运行启动浏览器，引导用户扫码登录抖音创作者平台
2. 登录成功后将完整Cookie序列化保存到本地JSON文件
3. 每次任务启动前加载Cookie，访问首页检测登录态
4. 登录失效时自动触发扫码登录流程
5. 固定浏览器指纹与User-Agent，减少异地登录风险

### 5.2 内容合规校验方案

采用「关键词初筛 + LLM复检」两级校验：

1. 第一级：本地敏感词库前缀树匹配，快速拦截明确违规内容
2. 第二级：调用大模型进行语义合规检测，识别隐性违规
3. 校验不通过直接终止流程，返回违规原因
4. 校验节点：文案生成后、视频渲染前各执行一次

### 5.3 视频渲染性能方案

1. 素材本地缓存，相同关键词复用已有素材
2. TTS语音与字幕生成并行处理
3. FFmpeg硬件加速编码（支持NVIDIA/Intel核显）
4. 渲染任务单队列执行，避免CPU占用过高

### 5.4 风控规避方案

1. 操作延迟：每个点击/输入操作前加入3~10秒随机延迟
2. 行为模拟：鼠标移动采用贝塞尔曲线轨迹，输入文本模拟打字速度
3. 频率控制：单日发布不超过配置阈值，两次发布间隔≥2小时
4. 环境固定：固定IP、浏览器指纹、登录设备，减少异常检测
5. 失败熔断：连续发布失败3次自动终止当日任务

## 6. 异常处理与重试机制

- **网络异常**：API请求、页面加载失败自动重试3次，指数退避间隔
- **元素定位失败**：重试2次，仍失败则截图保存现场，抛出异常
- **渲染失败**：清理临时文件，记录错误日志，终止当前任务
- **发布失败**：保存发布参数，支持手动重试，不自动重复发布
- **全局异常捕获**：调度层统一捕获所有异常，记录日志，不崩溃退出

------

# 三、开发实现文档

## 1. 开发环境准备

### 1.1 环境要求

- 操作系统：Windows 10/11、macOS 11+、Linux 均可
- Python版本：3.10 及以上
- 依赖工具：FFmpeg（视频渲染必需）
- 浏览器：Playwright 自动管理 Chromium

### 1.2 基础依赖安装

```
# 升级pip
pip install --upgrade pip

# 安装核心依赖
pip install requests beautifulsoup4 playwright apscheduler pyyaml moviepy edge-tts

# 安装Playwright浏览器
playwright install chromium

# 安装FFmpeg（Windows推荐用scoop，macOS用brew）
# Windows: scoop install ffmpeg
# macOS: brew install ffmpeg
```

## 2. 项目目录结构

```
douyin-auto-content/
├── config/                  # 配置目录
│   ├── settings.yaml        # 全局配置文件
│   ├── sensitive_words.txt  # 敏感词库
│   └── accounts.yaml        # 账号配置（git忽略）
├── src/                     # 源码目录
│   ├── common/              # 通用支撑层
│   │   ├── __init__.py
│   │   ├── config.py        # 配置管理
│   │   ├── logger.py        # 日志系统
│   │   ├── retry.py         # 重试装饰器
│   │   └── compliance.py    # 合规校验
│   ├── hot_analysis/        # 热点分析模块
│   │   ├── __init__.py
│   │   ├── crawler.py       # 热点采集
│   │   └── selector.py      # 热点选题
│   ├── content_gen/         # 文案生成模块
│   │   ├── __init__.py
│   │   ├── prompts/         # Prompt模板目录
│   │   │   └── script_gen.tpl
│   │   ├── llm_client.py    # LLM客户端
│   │   └── script_writer.py # 脚本生成器
│   ├── video_render/        # 视频渲染模块
│   │   ├── __init__.py
│   │   ├── tts.py           # 语音合成
│   │   ├── material.py      # 素材服务
│   │   ├── subtitle.py      # 字幕生成
│   │   └── composer.py      # 视频合成
│   ├── douyin_adapter/      # 抖音适配器
│   │   ├── __init__.py
│   │   ├── account.py       # 账号管理
│   │   ├── risk_control.py  # 风控引擎
│   │   └── publisher.py     # 发布执行
│   └── scheduler/           # 调度模块
│       ├── __init__.py
│       └── task_scheduler.py
├── cache/                   # 缓存目录（git忽略）
│   ├── material/            # 素材缓存
│   └── temp/                # 临时文件
├── data/                    # 数据目录（git忽略）
│   ├── logs/                # 日志文件
│   └── works.db             # 作品数据库
├── output/                  # 输出视频目录（git忽略）
├── main.py                  # 手动执行入口
├── run_scheduler.py         # 调度服务入口
├── requirements.txt         # 依赖清单
└── README.md                # 项目说明
```

## 3. 核心模块开发指南

### 3.1 通用支撑层开发

#### 配置管理（config.py）

采用单例模式，统一读取YAML配置，支持热加载。

```
import yaml
from pathlib import Path

class ConfigManager:
    _instance = None
    
    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
            cls._instance._load_config()
        return cls._instance
    
    def _load_config(self):
        config_path = Path("config/settings.yaml")
        with open(config_path, "r", encoding="utf-8") as f:
            self.config = yaml.safe_load(f)
    
    def get(self, key: str, default=None):
        keys = key.split(".")
        value = self.config
        for k in keys:
            if isinstance(value, dict):
                value = value.get(k)
            else:
                return default
        return value if value is not None else default
```

### 3.2 热点分析模块开发

#### 热点采集（crawler.py）

抓取抖音网页版热榜，解析结构化数据。

```
import requests
from bs4 import BeautifulSoup
from src.common.retry import retry
from dataclasses import asdict

class HotCrawler:
    def __init__(self):
        self.url = "[https://www.douyin.com/hot](https://www.douyin.com/hot)"
        self.headers = {
            "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
        }
    
    @retry(max_retries=3, delay=2)
    def fetch_hot_list(self) -> list:
        resp = requests.get(self.url, headers=self.headers, timeout=10)
        resp.raise_for_status()
        soup = BeautifulSoup(resp.text, "html.parser")
        
        hot_items = []
        # 根据实际页面结构解析，以下为示例逻辑
        items = soup.select(".hot-item")
        for idx, item in enumerate(items, 1):
            title = item.select_one(".title").get_text(strip=True)
            hot_value = int(item.select_one(".hot-value").get_text(strip=True))
            hot_items.append(HotItem(
                rank=idx,
                title=title,
                hot_value=hot_value,
                category="热点",
                url="",
                tags=[]
            ))
        return hot_items
```

### 3.3 文案生成模块开发

#### 脚本生成器（script_writer.py）

调用LLM生成结构化分镜脚本。

```
import json
from src.content_gen.llm_client import LLMClient
from src.common.config import ConfigManager

class ScriptWriter:
    def __init__(self):
        self.llm = LLMClient()
        self.config = ConfigManager()
        self.prompt_template = self._load_prompt()
    
    def _load_prompt(self):
        with open("src/content_gen/prompts/script_gen.tpl", "r", encoding="utf-8") as f:
            return f.read()
    
    def generate_script(self, topic: SelectedTopic) -> VideoScript:
        prompt = self.prompt_template.format(
            hot_title=topic.hot_item.title,
            create_angle=topic.create_angle,
            duration=self.config.get("video.default_duration", 60)
        )
        
        result = self.llm.chat(prompt, response_format="json")
        data = json.loads(result)
        
        scenes = [Scene(**scene) for scene in data["scenes"]]
        return VideoScript(
            topic=topic,
            title=data["title"],
            full_script=data["full_script"],
            tags=data["tags"],
            scenes=scenes,
            total_duration=data["total_duration"],
            cover_text=data["cover_text"]
        )
```

### 3.4 视频渲染模块开发

#### 视频合成器（composer.py）

基于MoviePy合成最终视频。

```
from moviepy.editor import *
from pathlib import Path
from src.video_render.tts import TTSEngine
from src.video_render.material import MaterialService
from src.video_render.subtitle import SubtitleGenerator

class VideoComposer:
    def __init__(self):
        self.tts = TTSEngine()
        self.material = MaterialService()
        self.subtitle_gen = SubtitleGenerator()
        self.output_dir = Path("output")
        self.output_dir.mkdir(exist_ok=True)
    
    def compose(self, script: VideoScript) -> RenderResult:
        # 1. 生成语音
        audio_path, duration = self.tts.generate(script.full_script)
        
        # 2. 匹配素材
        image_paths = []
        for scene in script.scenes:
            img_path = self.material.get_image(scene.image_keyword)
            image_paths.append(img_path)
        
        # 3. 生成字幕
        subtitle_clips = self.subtitle_gen.generate(script.scenes)
        
        # 4. 合成画面
        clips = []
        for i, scene in enumerate(script.scenes):
            img_clip = ImageClip(image_paths[i]).set_duration(scene.duration)
            clips.append(img_clip)
        
        video = concatenate_videoclips(clips, method="compose")
        audio = AudioFileClip(audio_path)
        video = video.set_audio(audio)
        
        # 5. 叠加字幕
        video = CompositeVideoClip([video] + subtitle_clips)
        
        # 6. 输出文件
        output_path = self.output_dir / f"{script.title[:10]}.mp4"
        video.write_videofile(str(output_path), fps=30, codec="libx264")
        
        return RenderResult(
            video_path=str(output_path),
            cover_path=image_paths[0],
            duration=duration,
            resolution=(1080, 1920)
        )
```

### 3.5 抖音适配器模块开发

#### 发布执行（publisher.py）

基于Playwright实现自动发布。

```
from playwright.sync_api import sync_playwright, Page
from src.douyin_adapter.account import AccountManager
from src.douyin_adapter.risk_control import RiskController

class VideoPublisher:
    def __init__(self):
        self.account_mgr = AccountManager()
        self.risk_ctrl = RiskController()
        self.publish_url = "[https://creator.douyin.com/creator-micro/content/upload](https://creator.douyin.com/creator-micro/content/upload)"
    
    def publish(self, render_result: RenderResult, script: VideoScript) -> PublishResult:
        with sync_playwright() as p:
            browser = p.chromium.launch(headless=False)
            context = browser.new_context(storage_state=self.account_mgr.cookie_path)
            page = context.new_page()
            
            # 风控：随机延迟
            self.risk_ctrl.random_delay()
            
            # 进入发布页
            page.goto(self.publish_url)
            page.wait_for_load_state("networkidle")
            
            # 上传视频
            page.set_input_files("input[type='file']", render_result.video_path)
            
            # 等待上传完成
            page.wait_for_selector(".upload-success", timeout=120000)
            
            # 填写标题
            self.risk_ctrl.human_type(page, "textarea[placeholder='填写标题']", script.title)
            
            # 添加话题
            for tag in script.tags:
                self.risk_ctrl.human_type(page, ".tag-input", f"#{tag} ")
                self.risk_ctrl.random_delay(1, 2)
            
            # 点击发布
            self.risk_ctrl.random_delay(3, 5)
            page.click(".publish-btn")
            
            # 等待发布结果
            page.wait_for_selector(".publish-success", timeout=30000)
            
            # 保存Cookie
            context.storage_state(path=self.account_mgr.cookie_path)
            browser.close()
            
            return PublishResult(
                success=True,
                video_url="",
                publish_time="",
                error_msg=None
            )
```

### 3.6 调度模块开发

基于APScheduler实现定时任务。

```
from apscheduler.schedulers.blocking import BlockingScheduler
from src.common.config import ConfigManager
from src.hot_analysis.crawler import HotCrawler
from src.hot_analysis.selector import HotSelector
from src.content_gen.script_writer import ScriptWriter
from src.video_render.composer import VideoComposer
from src.douyin_adapter.publisher import VideoPublisher

def full_workflow():
    """全流程任务"""
    try:
        # 1. 抓取热点
        crawler = HotCrawler()
        hot_list = crawler.fetch_hot_list()
        
        # 2. 筛选选题
        selector = HotSelector()
        topic = selector.select_best(hot_list)
        
        # 3. 生成脚本
        writer = ScriptWriter()
        script = writer.generate_script(topic)
        
        # 4. 合规校验
        # ...
        
        # 5. 渲染视频
        composer = VideoComposer()
        render_result = composer.compose(script)
        
        # 6. 发布视频
        publisher = VideoPublisher()
        result = publisher.publish(render_result, script)
        
        # 7. 记录结果
        # ...
        
    except Exception as e:
        logger.error(f"全流程执行失败: {str(e)}", exc_info=True)

def run_scheduler():
    config = ConfigManager()
    scheduler = BlockingScheduler()
    
    # 添加定时任务，每天8点、12点、18点执行
    cron = config.get("scheduler.cron", "0 8,12,18 * * *")
    scheduler.add_job(full_workflow, "cron", cron_expression=cron)
    
    scheduler.start()
```

## 4. MVP最小版本落地路线

### 阶段一：基础闭环（1~3天）

1. 实现热点采集 + 原始热榜输出
2. 实现文案生成 + 结构化脚本输出
3. 实现基础视频渲染（单图+TTS+字幕）
4. 手动执行全流程，发布环节人工操作

### 阶段二：自动化闭环（1周）

1. 实现抖音自动发布功能
2. 接入定时调度
3. 完善日志与配置管理
4. 加入基础合规校验

### 阶段三：效果优化（2周）

1. 实现多图分镜匹配 + 转场特效
2. 完善选题评分算法
3. 加入风控行为模拟
4. 作品数据本地记录