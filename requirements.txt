# 抖音自动化内容生产系统（个人版）依赖清单
# 推荐使用 uv 管理：依赖声明见 pyproject.toml，锁定版本见 uv.lock，直接 `uv sync` 即可
# 本文件仅作为不使用 uv 时的 pip 备选安装清单：python -m venv .venv && pip install -r requirements.txt
# Python 3.10+；建议在虚拟环境中安装

# ---- 基础 ----
requests>=2.31.0            # 网络请求
beautifulsoup4>=4.12.0      # DOM 解析（热点采集备用）
PyYAML>=6.0                 # 配置解析
APScheduler>=3.10,<4        # 定时调度

# ---- 视频渲染 ----
moviepy>=2.0.0              # 视频合成（自动携带 imageio-ffmpeg 的 ffmpeg）
edge-tts>=6.1.0             # 微软免费语音合成
Pillow>=10.0.0              # 图片处理 / 字幕渲染 / 占位图
numpy>=1.26.0               # moviepy 依赖

# ---- 浏览器自动化（发布） ----
playwright>=1.40.0
